import 'package:flutter/material.dart';

/// M06 — Anotações. Placeholder; lista global/export virão no módulo.
class AnnotationsScreen extends StatelessWidget {
  const AnnotationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Anotações')),
      body: const Center(child: Text('Anotações — em construção (M06)')),
    );
  }
}
