import 'package:flutter/material.dart';

/// M08 — Preferências de leitura. Placeholder; tema/fonte/espaçamento no módulo.
class ReaderSettingsScreen extends StatelessWidget {
  const ReaderSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Leitura')),
      body: const Center(child: Text('Preferências de leitura — em construção (M08)')),
    );
  }
}
