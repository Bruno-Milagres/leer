import 'package:flutter/material.dart';

/// M08 — Cadastro/validação de servidores. Placeholder; formulário e teste de
/// conexão virão no módulo.
class ServerSettingsScreen extends StatelessWidget {
  const ServerSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Servidor')),
      body: const Center(child: Text('Servidores — em construção (M08)')),
    );
  }
}
